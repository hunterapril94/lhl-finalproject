import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#05386B"
    },
    secondary: {
      main: "#8EE4AF",
      light: "#EDF5E1"
    },
    tertiary: {
      main: "#EDF5E1"
    }
  }
});

export default theme;